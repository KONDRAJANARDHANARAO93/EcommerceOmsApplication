spring:
  application:
    name: ecommerce-oms

  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_emo
    username: postgres
    password: root123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate   # change from validate for now
    show-sql: true
    properties:
      hibernate:
        format_sql: true

